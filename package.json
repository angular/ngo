{
  "name": "ngo-loader",
  "version": "0.0.3",
  "main": "dist/index.js",
  "license": "MIT",
  "scripts": {
    "build": "rm -rf dist/ && tsc",
    "pretest": "npm run build",
    "test": "node dist/test.js input.js > res.js"
  },
  "dependencies": {
    "typescript": "2.1"
  },
  "devDependencies": {
    "@types/node": "^7.0.5",
    "ts-node": "^2.1.0"
  }
}
