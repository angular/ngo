import { oneLine } from 'common-tags';

import { purify } from './purify';


// TODO: add purify tests
describe('purify', () => {
  it('TODO', () => {
    const input = oneLine`
    `;
    const output = oneLine`
    `;

    expect(oneLine`${purify(input)}`).toEqual(output);
  });
});
